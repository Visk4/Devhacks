spring.application.name=backend

# ========================
# MySQL Database
# ========================
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/codestorm?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&useUnicode=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:1853}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# ========================
# JPA
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ========================
# Flyway
# ========================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true


jwt.secret=thisIsAVeryLongSecretKeyForJWTSigningThatShouldBeAtLeast256BitsOrMoreForHS512Algorithm
jwt.expiration=86400000
jwt.refresh.expiration=604800000

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
oauth.enabled=true
oauth.success-redirect=${OAUTH_SUCCESS_REDIRECT:http://localhost:5173/oauth2/callback}
security.cookie.secure=${COOKIE_SECURE:false}

# ========================
# OAuth2 Google
# ========================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:126439899123-u6f56799vv2992om8pnr9euggf7cqo69.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=openid,profile,email

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shivsharansanjawad@gmail.com
spring.mail.password=nkxtknljmnrzeuiq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
mail.enabled=false


app.upload-dir=uploads
spring.web.resources.static-locations=classpath:/static/,file:./uploads/

app.public-base-url=

# RabbitMQ removed for local dev; using in-process job queue

# ========================
# Redis
# ====


# ========================
# Gemini AI (Code Hints)
# ========================
gemini.api.key=${GEMINI_API_KEY:AIzaSyBVhnMEbzc7-hjss6Mq9T-REzgv7TZlkyU}
gemini.model=gemini-2.5-flash